nutch-content-filter
====================

An Apache Nutch plugin to filter pages based on their content (on their text). The first approach is pretty simple, just filtering documents where any of the desired words is present
